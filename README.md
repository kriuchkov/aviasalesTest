## Тестовое задание в команду ассистеда (Python/Go)

В папке два XML – это ответы на поисковые запросы, сделанные к одному из наших партнёров.
В ответах лежат варианты перелётов (тег `Flights`) со всей необходимой информацией,
чтобы отобразить билет на Aviasales.

На основе этих данных, нужно сделать вебсервис,
в котором есть эндпоинты, отвечающие на следующие запросы:

* Какие варианты перелёта из DXB в BKK мы получили?
* Самый дорогой/дешёвый, быстрый/долгий и оптимальный варианты
* В чём отличия между результатами двух запросов (изменение маршрутов/условий)?

Язык реализации: `Go`
Формат ответа: `json`
По возможности использовать стандартную библиотеку.

Язык реализации: `python3`
Формат ответа: `json`
Используемые библиотеки и инструменты — всё на твой выбор.

Оценивать будем умение выполнять задачу имея неполные данные о ней,
умение самостоятельно принимать решения и качество кода.


## Реализация

Созданы два вебсервиса, один на `Go`, другой на `python3`. Каждый имеет две точки входа:

* `/receive` - прием XML данные и загрузка их во временное хранилище
* `/itinerary` - вывод маршрутов с перелетами по заданным параметрам

## Контекст itinerary

**source**: string
	Точка отправки

**destination**: string
	Точка прилета

**type**: string
	Показать оптимальный маршрут, варианты:

	* stime: самый долгий маршрут
	* ltime: самый быстрый маршрут
	* sprice: самый дорогой маршрут
	* lprice: самый дешевый маршрут
	* optimal: Оптимальный маршрут по соотношению цены/качество

**return**: bool 
	Показать маршруты с обратным перелетом


## Запуск и проверка 

* `make pyrun` - Запускается flask через pipenv 
* `make gorun` - Собирается бинарник и запускается гошный код


## Ответы на вопросы

**Почему выбрана сортировка через очередь или кучу с приоритетом?**

Так было удобно, самая удобная реализация конечно на python, 
но `go` код с помощью интерфейсом избавил нас от много лишнего 

**Почему было выбрано такое решение поиска маршрута?**

Я счел, что маршрут не отделим от перелета и они единое целое, 
искать по обратному маршруту точки, тоже счел излишним. 
Да, решение было реализовано в лоб и не масштабируемое, но любую
неопределенность я использовал в свою пользу. Упрощать.


